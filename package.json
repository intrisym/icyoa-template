{
  "name": "icyoa-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check:js": "node --check script.js && node --check editor.js && node --check logicExpr.js && node --check server.js && node --check generate-manifest.js",
    "test:cyoas": "node scripts/validate-cyoas.js",
    "test:functional": "node scripts/functional-cyoa-tests.js",
    "verify": "npm run check:js && npm run test:cyoas && npm run test:functional",
    "test": "npm run verify"
  }
}
